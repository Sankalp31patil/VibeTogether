# Sets the port on which the Spring Boot application will run
server.port=9090

# Binds the server to all available network interfaces (not just localhost)
# Useful for accessing the server from other devices on the same network
server.address=0.0.0.0
